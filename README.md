# IAM Demo

AWS login workflow with IAM role management 

## Installation
```
brew install awscli
aws configure
pip install aws-mfa
```

## Usage

```javascript
login_aws
assume_role infosec
unset_role
assume_role dev
```
